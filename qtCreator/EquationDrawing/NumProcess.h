#ifndef NUMPROCESS_H
#define NUMPROCESS_H
#include <iostream>
#include <sstream>
#include <string>
#include <vector>
#include <regex>
#include <stack>
#include <map>
#include <algorithm>
#include <cstdlib>
#include <sstream>
#include <QString>

using std::cin;
using std::cout;
using std::getline;
using std::endl;
using std::map;
using std::string;
using std::vector;
using std::regex;
using std::stack;
/*
class Calculator {
    static int weight(char op);
public:
    static map<string, double> exist_var;
    string show_exist;
    double RUN(QString equation, bool can_draw);
    static double Input(bool& can_draw, string inputStr);
    static string judgeFormat(string infix); //判斷名字 若有變數則直接替換

    static double calculate(string posfix);
    static bool isVariable(string str);
    static auto is_Var_exist(string name);

    static bool judge_if_can_draw(QString equation);

    static string InfixtoPosfix(string infix);
    static double Output(double ans);
    void test();
};
*/
#endif // NUMPROCESS_H
